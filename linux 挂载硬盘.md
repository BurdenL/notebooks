在 Ubuntu 上挂载 NTFS 硬盘，可以按照以下步骤进行：

### 1. 安装 NTFS 文件系统支持

Ubuntu 默认支持 NTFS 文件系统，但如果你没有安装 `ntfs-3g`（一个用于访问 NTFS 分区的驱动程序），你可以使用以下命令安装它：
```cmd
sudo apt update
sudo apt install ntfs-3g
```

### 2. 查找 NTFS 硬盘

插入 NTFS 硬盘后，使用以下命令查找硬盘设备的名称（例如 `/dev/sdb1`）：

```cmd
sudo fdisk -l
```

该命令会列出所有连接的硬盘及其分区，查找你的 NTFS 分区。比如，如果你看到 `/dev/sdb1` 这个分区，它可能是你的 NTFS 硬盘分区。

### 3. 创建挂载点

在你挂载 NTFS 分区之前，需要先创建一个挂载点（一个目录）。你可以选择任何目录作为挂载点，常见的是 `/mnt` 或 `/media` 下。

```cmd
sudo mkdir /mnt/ntfs
```

### 4. 挂载 NTFS 硬盘

使用 `mount` 命令挂载 NTFS 分区。假设你的硬盘设备是 `/dev/sdb1`，挂载点是 `/mnt/ntfs`，你可以执行以下命令：

```cmd
sudo mount -t ntfs-3g /dev/sdb1 /mnt/ntfs
```

### 5. 确认挂载成功

挂载完成后，你可以通过以下命令检查是否成功挂载：

```cmd
df -h
```

或者，你也可以进入挂载点目录，查看文件内容：
```cmd
cd /mnt/ntfs
ls
```

### 6. 设置开机自动挂载（可选）

如果你希望在系统启动时自动挂载 NTFS 硬盘，可以编辑 `/etc/fstab` 文件。

打开文件：

```cmd
sudo nano /etc/fstab
```

然后添加一行，指定 NTFS 分区的设备和挂载点。例如，假设你的 NTFS 分区是 `/dev/sdb1`，挂载点是 `/mnt/ntfs`，你可以添加如下内容：
```cmd
/dev/sdb1  /mnt/ntfs  ntfs-3g  defaults  0  0
```

保存并关闭文件（按 `Ctrl + O` 保存，按 `Ctrl + X` 退出）。

下次启动时，NTFS 硬盘会自动挂载。



## 使用完成后，如何安全退出



###### 在 Ubuntu 上使用完 NTFS 硬盘后，正确地卸载它非常重要，这可以防止数据丢失或文件系统损坏。你可以按照以下步骤安全退出：

### 1. 退出当前目录

首先，如果你正在使用 NTFS 硬盘中的文件，确保已经退出所有相关目录和文件：

```cmd
cd ~
```

或者进入其他目录。

### 2. 卸载硬盘

使用 `umount` 命令卸载硬盘分区。例如，如果你挂载的 NTFS 硬盘在 `/mnt/ntfs` 目录下，执行以下命令：

```cmd
sudo umount /mnt/ntfs
```

如果分区正被使用，可能会提示无法卸载。这时你可以使用以下命令来查看哪些进程正在使用该挂载点：
```cmd
sudo lsof /mnt/ntfs
```

然后，结束这些进程或在没有活动的情况下再次尝试卸载。

### 3. 使用 `-l` 参数强制卸载（如果必要）

如果常规的 `umount` 无法成功，尝试使用 `-l`（lazy）参数强制卸载：

```cmd
sudo umount -l /mnt/ntfs
```

### 4. 卸载完成后，安全移除硬盘

一旦成功卸载硬盘，你可以安全地拔出 NTFS 硬盘。如果你是在物理计算机上操作，确保硬盘在没有任何读写操作时被拔出。

### 5. 如果你是通过图形界面操作

如果你是在 Ubuntu 的文件管理器中操作 NTFS 硬盘，你可以右键点击硬盘图标，选择“弹出”或者“卸载”，然后等待弹出操作完成，确保硬盘不再进行任何读写操作，再安全拔出硬盘。